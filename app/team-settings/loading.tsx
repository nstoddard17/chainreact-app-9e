import { DashboardLoading } from "@/components/common/DashboardLoading"

export default function Loading() {
  return <DashboardLoading title="Loading team settings" subtitle="Preparing member controls..." statCards={2} />
}
