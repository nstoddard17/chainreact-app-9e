"use client"

import { TempPlaceholder } from "@/components/temp/TempPlaceholder"

export default function OAuthCallbackTempPage() {
  return (
    <TempPlaceholder
      type="marketing"
      title="Connecting your account"
      description="OAuth callbacks use the refreshed microcopy and progress indicators to reassure users mid-flow."
    />
  )
}

