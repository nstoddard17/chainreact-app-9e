import { DashboardLoading } from "@/components/common/DashboardLoading"

export default function Loading() {
  return <DashboardLoading title="Loading teams" subtitle="Fetching your teams and invitations..." statCards={3} />
}
