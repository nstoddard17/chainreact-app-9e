import { DashboardLoading } from "@/components/common/DashboardLoading"

export default function Loading() {
  return <DashboardLoading title="Loading organization settings" subtitle="Loading billing and workspace controls..." statCards={2} />
}
