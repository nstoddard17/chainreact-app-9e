export type { AutoMappingParams, AutoMappingEntry } from '@/lib/workflows/autoMapping'

export {
  computeAutoMappingEntries,
  computeAutoMappingSuggestion,
  extractNodeOutputs,
  sanitizeAlias,
  applyAutoMappingSuggestions
} from '@/lib/workflows/autoMapping'
