import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export function DeploymentManagement() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Deployment Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Manage your workflow deployments</p>
      </CardContent>
    </Card>
  )
}
